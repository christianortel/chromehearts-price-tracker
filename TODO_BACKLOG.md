# TODO BACKLOG

## Deferred

- add object storage provider abstraction with signed upload URLs for submission proofs and other stored assets
- add user-defined saved browse views beyond the current curated quick-view presets and copy-link flow
- upgrade the web app from `next@15.1.7` to a patched release after validating any breaking config changes
- add richer seller reputation weighting in price confidence scoring
- add semantic embedding fallback for ambiguous matching when an embedding provider is configured
- expand curated seller coverage to Justin Reed and Grailed after selector validation
- add alert delivery channels beyond in-app records
- add object-storage-backed artifact delivery for admin parse-failure inspection
- add screenshot thumbnail browser in admin parse-failure view
- add source-specific backfill scheduling policies
- add stronger admin RBAC beyond single-role token auth
- add richer admin catalog filters and scoped product search facets beyond the new free-text lookup
- add richer duplicate-group merge semantics and reviewer notes beyond single-keeper resolution
- add real sold-comp adapter support once a compliant reliable source is confirmed
